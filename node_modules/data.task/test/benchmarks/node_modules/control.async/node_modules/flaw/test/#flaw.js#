var expect = require('chai').expect
var flaw = require('../')

describe('{} Flaw', function() {
  describe('λ make-from', function() {
    it('Should be an instance of the given error type.', function() {
      var e = flaw.from(SyntaxError, '', '')
      expect(e).instanceOf(SyntaxError)
    })
    it('Should have the message it\'s given.', function() {
      
    })
    it('Should have the name it\'s given.')
    it('Should have a proper stack trace.')
  })

  describe('λ make', function() {
    it('Should be an instance of Error.')
    it('Should have the options it\'s given.')
  })

  describe('λ raise', function() {
    it('Should throw the error.')
  })
})